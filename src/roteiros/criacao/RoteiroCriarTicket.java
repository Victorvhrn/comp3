package roteiros.criacao;

public class RoteiroCriarTicket {
	public void execute(boolean pago, int consumidorId, int refeicaoId){
		/*
		 * Consumidor consumidor = new DMConsumidor().selectById(consumidorId);
		 * Refeicao refeicao = new DMRefeicao().selectById(refeicaoId);
		 * new DMTicket().insert(new Ticket(pago,consumidor,refeicao));
		 */
	}
}

