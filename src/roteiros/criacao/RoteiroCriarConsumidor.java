package roteiros.criacao;

import entidades.Sexo;
import entidades.Titulo;

public class RoteiroCriarConsumidor {
	public void execute(String nome, int matricula, String anoIngresso, Sexo sexo,
						Titulo titulo,String cpf, int cursoId,int departamentoId){
		/*
		 * DMConsumidor dmConsumidor = new DMConsumidor();
		 * Consumidor consumidor;
		 * if (cursoId != null){
		 * 		DMCurso dmCurso = new DMCurso();
		 * 		Curso curso = dmCurso.selectById(cursoId);
		 * 		consumidor = new Consumidor(nome,matricula,anoIngresso,sexo,titulo,cpf,curso);
		 * 		
		 * }else if( departamentoId != null){
		 * 		DMDepartamento dmDepartamento = new DMDepartamento();
		 * 		Departamento departamento = dmDepartamento.selectById(departamentoId);
		 * 		consumidor = new Consumidor(nome,matricula,anoIngresso,sexo,titulo,cpf,departamento);
		 * 		
		 * }else{
		 * 		throw new CriacaoException("Erro na criação do Consumidor");
		 * } 
		 * dmConsumidor.insert(consumidor);
		 * 
		 */
		
	}
}
